
export component SettingsPage inherits Rectangle {
    width: 100%;
    height: 100%;
    background: #f7ede0; // Light orange

    VerticalLayout {
        alignment: center;
        spacing: 15px;

        Text { text: "Settings"; font-size: 32px; font-weight: 700; }
        Text { text: "Customize your preferences."; }
    }
}
